import { ScrollView, Text, View } from "react-native";

export default function OfferScreen() {
    return(
        <ScrollView>
            <View>
                <Text>Test</Text>
            </View>
        </ScrollView>
    )
}